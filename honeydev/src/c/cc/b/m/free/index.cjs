'use strict';
// cc/b/m/free — bm-free: back module entry / error export
// Packages the cc-free error schema for use by the rest of the cc frame.

const { ErrorKind, HdError } = require('../b/free/error.cjs');

module.exports = { ErrorKind, HdError };
